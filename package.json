{
  "name": "lentl",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world  && cp src/index.html build/index.html && cp -r src/css build/css",
    "start": "cp src/index.html build/index.html && cp -r src/css build/css && bsb -make-world -w",
    "clean": "bsb -clean-world",
    "test": "echo \"Error: no test specified\" && exit 1",
    "webpack": "webpack -w",
    "webpack:production": "NODE_ENV=production webpack"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bs-css": "^7.2.0",
    "re-react-player": "^0.1.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "reason-react": "^0.5.3"
  },
  "devDependencies": {
    "bs-platform": "^4.0.0",
    "webpack": "^4.0.1",
    "webpack-cli": "^2.0.10"
  }
}